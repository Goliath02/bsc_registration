name: Build and Push Docker Image

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build Spring Boot + Vue and Docker Image

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Optional: Cache npm dependencies for frontend
      - name: Cache frontend dependencies
        uses: actions/cache@v4
        with:
          path: frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # Optional: Cache Maven dependencies for faster builds
      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # Build Docker image
      - name: Build Docker image
        run: docker build -t myapp:latest .

      # --- Optional: Push to Docker Hub ---
      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/master'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Push image to Docker Hub
        if: github.ref == 'refs/heads/master'
        run: |
          IMAGE_NAME=${{ secrets.DOCKER_USERNAME }}/bsc-registration
          docker tag goliath02/bsc_registration:latest $IMAGE_NAME:latest
          docker push $IMAGE_NAME:latest
